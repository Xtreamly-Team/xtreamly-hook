import { PositionHistoryRepository } from './position-history.repository';

describe('PositionHistoryRepository', () => {
  it('should be defined', () => {
    expect(new PositionHistoryRepository()).toBeDefined();
  });
});
